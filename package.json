{
  "name": "stoneforge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stoneforge V2 workspace root",
  "scripts": {
    "build": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run build; else echo \"No active V2 packages yet.\"; fi'",
    "coverage": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run coverage; else echo \"No active V2 packages yet.\"; fi'",
    "dev": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run dev; else echo \"No active V2 apps yet.\"; fi'",
    "test": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run test; else echo \"No active V2 packages yet.\"; fi'",
    "lint": "eslint \"packages/*/src/**/*.ts\" \"apps/*/src/**/*.ts\" \"scripts/**/*.mjs\" \".codex/hooks/**/*.mjs\" \".claude/hooks/**/*.mjs\" --no-error-on-unmatched-pattern && sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run lint; else echo \"No active V2 packages yet.\"; fi'",
    "lint:fix": "eslint \"packages/*/src/**/*.ts\" \"apps/*/src/**/*.ts\" \"scripts/**/*.mjs\" \".codex/hooks/**/*.mjs\" \".claude/hooks/**/*.mjs\" --fix --no-error-on-unmatched-pattern",
    "format:fix": "prettier . --write --ignore-unknown",
    "prepare": "husky",
    "quality": "pnpm lint && pnpm coverage && pnpm quality:deps && pnpm quality:crap && pnpm quality:structure && pnpm quality:type-api && pnpm quality:architecture",
    "quality:ci": "pnpm quality && pnpm mutation",
    "quality:crap": "node scripts/check-crap.mjs",
    "quality:deps": "depcruise --config .dependency-cruiser.cjs packages apps",
    "quality:architecture": "node scripts/check-architecture.mjs",
    "quality:architecture:update-baseline": "node scripts/check-architecture.mjs --update-baseline",
    "quality:fast": "pnpm lint && pnpm quality:deps && pnpm quality:structure && pnpm quality:type-api && pnpm quality:architecture",
    "quality:precommit": "pnpm quality",
    "quality:prepush": "pnpm quality:ci",
    "quality:turn": "pnpm quality:ci",
    "quality:structure": "node scripts/check-engineering-structure.mjs",
    "quality:type-api": "node scripts/check-type-driven-apis.mjs",
    "smoke:providers": "pnpm --filter @stoneforge/execution smoke:providers",
    "mutation": "stryker run stryker.conf.mjs",
    "mutation:dry": "stryker run stryker.conf.mjs --dryRunOnly",
    "typecheck": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run typecheck; else echo \"No active V2 packages yet.\"; fi'",
    "clean": "sh -lc 'if find apps packages -mindepth 2 -maxdepth 2 -name package.json -print -quit | grep -q .; then turbo run clean; fi; rm -rf node_modules'",
    "reference:v1:install": "pnpm --dir reference/v1 install",
    "reference:v1:build": "pnpm --dir reference/v1 build",
    "reference:v1:test": "pnpm --dir reference/v1 test",
    "reference:v1:dev": "pnpm --dir reference/v1 dev",
    "reference:v1:dev:smithy": "pnpm --dir reference/v1 dev:smithy",
    "reference:v1:dev:platform": "pnpm --dir reference/v1 dev:platform",
    "reference:smithy-next:install": "npm --prefix reference/smithy-next install",
    "reference:smithy-next:dev": "npm --prefix reference/smithy-next run dev",
    "reference:smithy-next:build": "npm --prefix reference/smithy-next run build"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stoneforge-ai/stoneforge.git"
  },
  "author": "Stoneforge",
  "license": "Apache-2.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@eslint/js": "^10.0.1",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/typescript-checker": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^4.0.18",
    "dependency-cruiser": "^17.3.10",
    "eslint": "^10.2.1",
    "eslint-plugin-sonarjs": "^4.0.3",
    "fast-check": "^4.3.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.8.14",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.59.0",
    "vitest": "^4.0.18"
  }
}
