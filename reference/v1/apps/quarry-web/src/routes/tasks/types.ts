/**
 * Types for the Tasks page
 * Re-exports shared types and defines page-specific types
 */

// Re-export shared types from lib for convenience
export type {
  Task,
  Entity,
  ViewMode,
  SortDirection,
  SortField,
  GroupByField,
  SortConfig,
  FilterConfig,
  TaskGroup,
} from '../../lib/task-constants';
