---
"@stoneforge/smithy-web": patch
---

Migrate responsive CSS custom property tokens (spacing, typography, layout) from viewport @media queries to @container queries so token values adapt based on container width instead of viewport width
