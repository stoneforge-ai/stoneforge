# Active V2 Packages

This directory is reserved for active Stoneforge V2 packages.

- Do not move V1 packages back here.
- Use `reference/v1/packages/` for archived V1 package code.
- Keep V2 package boundaries aligned with the docs in `docs/v2/`.
