{
  "name": "@stoneforge/execution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stoneforge V2 task dispatch, assignment, and session domain package",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "pnpm run typecheck",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stoneforge/core": "workspace:*",
    "@stoneforge/workspace": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
