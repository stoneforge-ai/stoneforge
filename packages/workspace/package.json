{
  "name": "@stoneforge/workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stoneforge V2 workspace onboarding and readiness domain package",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "pnpm run typecheck",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stoneforge/core": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
