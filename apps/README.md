# Active V2 Apps

This directory is reserved for active Stoneforge V2 applications.

- Do not move V1 apps back here.
- Use `reference/v1/apps/` for archived V1 app code.
- Use `reference/smithy-next/` for the prototype reference.
