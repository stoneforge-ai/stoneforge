---
"@stoneforge/smithy": minor
---

Remove legacy steward types (health, ops, reminder) from type definitions, UI components, CLI help text, prompt files, and documentation. StewardFocus now only supports 'merge' and 'docs'.
