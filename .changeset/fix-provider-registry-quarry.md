---
"@stoneforge/quarry": patch
---

Add createConfiguredProviderRegistry() utility that replaces placeholder providers with real configured providers when tokens are set. Fix CLI push handler to actually push via SyncEngine instead of just validating tasks.
