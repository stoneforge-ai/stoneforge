---
"@stoneforge/quarry": minor
---

Add Library select property to Notion document database schema for tagging documents with their library path (e.g., 'documentation/api-reference'). The property is auto-created if missing and supports both push (create/update) and pull paths.
