---
"@stoneforge/smithy-web": minor
---

Add container-aware responsive hooks (useContainerWidth, useContainerBreakpoint, useContainerIsMobile, useContainerIsTablet, useContainerIsDesktop) that track the main content container's width via ResizeObserver instead of the viewport width
