---
"@stoneforge/quarry": minor
---

Add `sf plan auto-complete` CLI command that scans all active plans and transitions those with 100% task completion to completed status. Includes `--dry-run` mode and `sweep` alias.
