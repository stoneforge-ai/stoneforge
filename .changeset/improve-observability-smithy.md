---
"@stoneforge/smithy": minor
---

Add configurable cost estimation to MetricsService with per-model pricing, per-task/agent aggregation queries, enhanced API route with cost data and groupBy=task|agent, operation log wiring into dispatch daemon and merge steward
