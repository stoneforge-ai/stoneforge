---
"@stoneforge/quarry": patch
---

Allow token-free providers (e.g., folder) in createProviderFromSettings. The folder provider no longer requires a token to be configured — only a project path is needed.
