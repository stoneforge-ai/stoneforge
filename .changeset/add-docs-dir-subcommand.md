---
"@stoneforge/quarry": minor
---

Add `sf docs dir` subcommand to quickly find the Documentation Directory document. Supports `--content` flag to include full markdown content, and standard `--json`/`--quiet` output modes.
