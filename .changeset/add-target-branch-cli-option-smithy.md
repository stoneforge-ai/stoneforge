---
"@stoneforge/smithy": minor
---

Add --target-branch option to `sf agent register` CLI command for director agents. The option allows specifying a target merge branch when registering a director. Also displays target branch in `sf agent show` output.
