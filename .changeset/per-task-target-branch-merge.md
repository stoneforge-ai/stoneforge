---
"@stoneforge/smithy": minor
---

Refactor merge steward to resolve target branch per-task from orchestrator metadata, enabling tasks to merge to different branches (e.g. staging) while preserving the global default fallback
