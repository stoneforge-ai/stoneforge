---
"@stoneforge/quarry": minor
---

Skip documents without a library by default in `link-all` and `push` commands. Add `--no-library` flag to opt-in to including library-less documents. Add `includeNoLibrary` option to `SyncOptions` for programmatic control.
