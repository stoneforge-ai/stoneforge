---
"@stoneforge/quarry": minor
---

Add autoLink and autoLinkProvider config fields to ExternalSyncConfig for automatic external issue creation on new tasks. Includes CLI commands `sf external-sync config set-auto-link <provider>` and `sf external-sync config disable-auto-link`.
