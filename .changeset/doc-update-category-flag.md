---
"@stoneforge/quarry": minor
---

Add `--category` flag to `sf document update` so operators can change a document's category from the CLI. The flag is validated against the same category set as `sf document create` and can be combined with `--content`, `--file`, or `--metadata`.
