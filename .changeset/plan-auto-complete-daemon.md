---
"@stoneforge/smithy": minor
---

Add plan auto-completion polling to dispatch daemon. Plans where all non-tombstone tasks are closed are now automatically marked as completed during the daemon's polling cycle.
