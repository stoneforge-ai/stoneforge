---
"@stoneforge/quarry": minor
---

Add `sf external-sync` CLI command with subcommands: config, config set-token, config set-project, link, unlink, push, pull, sync, status, and resolve. Supports --json, --quiet, and --verbose output modes.
