---
"@stoneforge/smithy": minor
---

Register recovery steward prompt in the prompt system: add 'recovery' to StewardFocus type, register steward-recovery.md in PROMPT_FILES, and update all validation, CLI, server routes, and test helpers to accept the new focus area.
