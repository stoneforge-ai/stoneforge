---
"@stoneforge/smithy": patch
---

Prune stale git worktree entries before creating new worktrees to prevent failures when a worktree directory was deleted but git's registry still has it registered
