---
"@stoneforge/quarry": minor
---

Add `sf external-sync link-all` command for bulk-linking all unlinked tasks to external issues. Supports `--provider`, `--project`, `--status`, `--dry-run`, and `--batch-size` flags with graceful rate limit handling.
