---
"@stoneforge/smithy-web": minor
---

Add provider change option to the provider install modal. Users can now switch agents to an installed provider directly from the modal instead of being required to install the missing provider.
