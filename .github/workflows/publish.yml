name: Publish Reference V1 (Paused)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Automatic publishing from the repository root is disabled after the V2 repo reorganization."
          echo "If V1 publishing is still required, restore a dedicated release workflow under reference/v1 before publishing."
